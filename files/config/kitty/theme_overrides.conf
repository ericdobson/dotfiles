# THEME OVERRIDES
#
# Appearance settings that should survive theme changes.
# Keep palette colors in the Tinty-generated theme file.

background_opacity 0.9
dynamic_background_opacity yes
background_blur 10
